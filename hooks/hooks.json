{
  "description": "oh-my-tbd hooks — narrow s3 safety check. Refuses only three chainsaw operations on trunk: git push --force, git reset --hard, git branch -D <trunk>. Everything else is conversation.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "node",
            "args": ["${CLAUDE_PLUGIN_ROOT}/bin/tbd.js", "hook", "safety-check"]
          }
        ]
      }
    ]
  }
}
